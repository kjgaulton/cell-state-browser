# Cell State Desktop App

Run the local desktop editor with:

```bash
python3 cell_state_desktop.py
```

Use the `File` menu to open, save, or save as JSON model files. Program
activities are shown for one selected state at a time and are sorted by activity
strength.
